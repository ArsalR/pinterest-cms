// mail module — Site Mailbox (V1.5 M1): Cloudflare Email Routing receives, a
// connected provider (Resend/Brevo/SendGrid) sends. Pure model + provider
// adapters + data layer + the signed inbound callback.
export * from "./model"
export * from "./providers"
export * from "./service"
export * from "./inboundRoutes"
