{
  "name": "sitenetwork-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/gen-redirects.mjs",
    "preview": "astro preview --port 4321",
    "check:zero-js": "node scripts/check-zero-js.mjs",
    "check:headers": "node scripts/check-headers.mjs",
    "lhci": "node scripts/lhci-run.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "wrangler": "^4.90.0"
  }
}
